/**
 * Flashcard components -- a minimal, physical-stack review experience.
 *
 * Replaces the four-button FSRS rating UI with a simpler two-button model:
 *   "Study Again" (FSRS Rating 1) and "Got It" (FSRS Rating 3).
 */

export { FlashcardStack } from './FlashcardStack';
export type { FlashcardStackProps, FlashcardStackStats } from './FlashcardStack';

export { Flashcard } from './Flashcard';
export type { FlashcardProps } from './Flashcard';
